{
  "name": "backend",
  "version": "1.0.0",
  "description": "json-server backend for angular crud application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 3001"
  },
  "author": "Flavio Alvarenga",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
